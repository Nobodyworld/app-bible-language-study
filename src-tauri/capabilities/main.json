{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege authority for the one Bible App Reader desktop window.",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:event:default",
    "core:window:allow-destroy",
    "desktop-vertical-slice"
  ]
}
